const fs = require('fs');
const map = JSON.parse(fs.readFileSync('scripts/asset-urls-freeimage.json', 'utf8'));
const g = (k) => (map[k] || {}).full || '';
const t = (k) => (map[k] || {}).thumb || (map[k] || {}).full || '';
const heroes = [
  { key: 'Electronics Deals', slug: '/categories/electronics', mobile: 'Electronic Deals' },
  { key: 'Gaming Essentials', slug: '/categories/gaming', mobile: 'Gaming Essentials' },
  { key: 'Home & Kitchen', slug: '/categories/home-kitchen', mobile: 'Home & Kitchen' },
  { key: 'Beauty Picks', slug: '/categories/beauty-personal-care', mobile: 'Beauty Picks' },
  { key: 'Office & productivity', slug: '/categories/office-productivity', mobile: 'Ofiice & Productivity' },
];
const strips = [
  ['beauty-personal-care', 'category banners/Beauty & Personal Care.png'],
  ['electronics', 'category banners/Electronics.png'],
  ['gaming', 'category banners/Gaming.png'],
  ['home-kitchen', 'category banners/Home & Kitchen.png'],
  ['office-productivity', 'category banners/Office.png'],
];
const trust = [
  ['Independently Reviewed', 'Trust Icons/Independently Reviewed.png'],
  ['Live Price Checks', 'Trust Icons/Live Price Checks.png'],
  ['Price History Tracking', 'Trust Icons/Price History Tracking.png'],
  ['Amazon Verified Links', 'Trust Icons/Amazon Verified Links.png'],
  ['Expert Buying Guides', 'Trust Icons/Expert Buying Guides.png'],
  ['Secure Affiliate Links', 'Trust Icons/Secure Affiliate Links.png'],
];
let out = `// AUTO-GENERATED by scripts/gen-brand-kit.cjs — DawnWire brand kit assets
// hosted on iili.io (freeimage.host). Regenerate after re-uploading.

export const BRAND_KIT = {
  heroes: [
${heroes.map(h => `    { desktop: '${g('Main Desktop hero banners/' + h.key + '.png')}', mobile: '${g('Mobile Hero Banner/' + h.mobile + '.png')}', href: '${h.slug}' },`).join('\n')}
  ],
  sidePromos: [
    { src: '${g('4 Side Promo Banners/Electronic deals.png')}', href: '/categories/electronics', alt: 'Electronic deals' },
    { src: '${g('4 Side Promo Banners/Beauty & Grooming.png')}', href: '/categories/beauty-personal-care', alt: 'Beauty & Grooming' },
    { src: '${g('4 Side Promo Banners/Home & Kitchen.png')}', href: '/categories/home-kitchen', alt: 'Home & Kitchen' },
    { src: '${g('4 Side Promo Banners/Best Sellers Top Rated.png')}', href: '/products?sort=rating', alt: 'Best Sellers Top Rated' },
  ],
  trustIcons: {
${trust.map(([name, k]) => `    '${name}': '${t(k)}',`).join('\n')}
  },
  featureBanners: {
    aiFinder: { src: '${g('Ai Finder.png')}', href: '/compare' },
    priceDrop: { src: '${g('Price drop.png')}', href: '/deals' },
  },
  categoryStrips: {
${strips.map(([slug, k]) => `    '${slug}': '${g(k)}',`).join('\n')}
  },
};
`;
fs.writeFileSync('src/lib/brandKit.ts', out);
console.log('written', out.length, 'bytes; sample hero:', BRANDKITCHECK = '');
