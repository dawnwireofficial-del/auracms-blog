name: Deploy to Vercel Production

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Restore upload manifest
        uses: actions/cache@v4
        with:
          path: .vercel-deploy-manifest.json
          key: vercel-upload-manifest-${{ github.ref }}

      - name: Deploy to production
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: team_XBNSPwcit0xkWNJb8CpPq6na
          VERCEL_PROJECT_ID: prj_St3lzIAB1KNKuzhloL6Sah078ScG
          VERCEL_PROJECT_NAME: auracms-blog
        run: node scripts/vercel-deploy.mjs